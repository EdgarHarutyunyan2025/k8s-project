project: "Example: single app multi envs"
version: "0.41.4"

releases:
  - name: my-release
    namespace: default
    chart:
      name: ../helm-chart
    values:
      - values.yml
    create_namespace: false
